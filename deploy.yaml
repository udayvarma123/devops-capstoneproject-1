apiVersion: apps/v1
kind: Deployment
metadata:
  name: my-app
  labels:
    app: apache
spec:
  replicas: 2
  selector:
    matchLabels:
      app: apache
  template:
    metadata:
      labels:
        app: apache
    spec:
      containers:
      - name: nginx
        image: udayvarma825/img2
        ports:
        - containerPort: 80
